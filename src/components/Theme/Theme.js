import {
    extendTheme,
    useColorModeValue
} from "@chakra-ui/react"

const theme = extendTheme({
    fonts: {
        heading: `'Asgalt', sans-serif`,
        body: `'Modeka', sans-serif`,
    },
})

export default theme;